# Wiring

Only connect the battery to `B+` and `B-`; do not connect cell taps. Connect the two motor wires to `M+` and `M-`, and the original low-current trigger to `TRIGGER` and logic ground as verified with a meter. Polarity and connector pinout are not yet verified for any specific blower.
