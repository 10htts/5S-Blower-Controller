# Firmware

Host-testable reference state machine. Build with a C++17 compiler when targeting hardware; constants are centralized in `src/config.h`. The current source is intentionally portable and does not claim a specific MCU pinout.
